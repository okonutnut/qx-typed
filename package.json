{
  "name": "base",
  "version": "5.0.1",
  "main": "index.html",
  "repository": {},
  "dependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "ag-grid-community": "^35.1.0",
    "basecoat-css": "^0.3.11",
    "http-server": "^14.1.1",
    "lucide-static": "^0.575.0",
    "tailwindcss": "^4.2.1"
  },
  "scripts": {
    "build": "npm run build-css && tsc",
    "build-dist": "npm run build && node scripts/build-dist.mjs",
    "build:dist": "npm run build-dist",
    "watch": "tsc -w --preserveWatchOutput",
    "serve:php": "php -S localhost:8080",
    "serve:reload": "browser-sync start --proxy \"localhost:8080\" --files \"lib/application.js, **/*.html, resource/app/styles.css\" --no-open",
    "dev": "concurrently -k \"npm:watch\" \"npm:serve:php\" \"npm:serve:reload\"",
    "build-css": "tailwindcss -i ./styles.css -o ./resource/app/styles.css --minify"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3",
    "undici-types": "^7.22.0"
  }
}
